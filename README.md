# Interaktive-3D-Visualisierung-von-Datenbanken
This project demonstrates how to load a large dataset (~10 million rows), analyze it using Python, and generate an interactive 3D visualization served via an Nginx web server running on my NAS using Docker.
